namespace = uu_sax_missions

country_event = {               #Porcelain in Meißen
	id = uu_sax_missions.1
	title = uu_sax_missions.1.t
	desc = uu_sax_missions.1.d
	picture = TRADE_GOODS_PLANTATION_GOODS_IN_WAREHOUSE_eventPicture
	goto = 61

	fire_only_once = yes
	
	major = yes

	trigger = {
		mission_completed = emp_sax_meissen_porcelain
	}
	
	mean_time_to_happen = {
		months = 1
	}

	immediate = {
		hidden_effect = { set_global_flag = bottger_happened }
	}
	
	option = {
		name = uu_sax_missions.1.a
		61 = {
			change_trade_goods = chinaware
			add_permanent_province_modifier = {
				name = uu_meissen_porcelain
				duration = -1
			}
			if = {
				limit = {
					has_building = weapons
				}
				hidden_effect = { remove_building = weapons }
				add_building = mills
			}
			else_if = {
				limit = {
					NOT = { has_building = mills}
				}
				add_building = mills
			}
			if = {
				limit = {
					is_institution_enabled = manufactories
					NOT = { manufactories = 100}
				}
				add_institution_embracement = {
					which = manufactories
					value = 100
				}
			}
			custom_tooltip = uu_sax_manufactories_institution.tt
		}
		change_price = {
			trade_goods = chinaware
			key = JOHAN_FRIEDRICH_BOTTGER
			value = -0.5
			duration = -1
		}
		add_prestige_or_monarch_power = { amount = 50 }
	}
}